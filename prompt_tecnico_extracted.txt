PROMPT TÉCNICO PARA IA -Geração de Código do Portal da Rota BioceânicaDocumento Word com prompt técnico otimizado para ChatGPT, Claude e GitHub CopilotObjetivo: fornecer uma instrução completa, reutilizável e pronta para colar em uma IA generativa para produzir o código do portal em arquitetura full-stack.Versão: 1.0Data: 29/04/2026Como usar este documentoO conteúdo abaixo já está estruturado como um prompt técnico completo. Basta copiar e colar o bloco principal em uma IA de geração de código, como ChatGPT, Claude ou GitHub Copilot Chat.Se desejar resultados ainda melhores, envie o prompt em etapas, pedindo primeiro a estrutura do projeto, depois o backend, o frontend, a infraestrutura e, por fim, os testes e o deploy.Prompt técnico pronto para copiar e colarATUE COMO UM DESENVOLVEDOR FULL-STACK SÊNIOR ESPECIALIZADO EM REACT.JS, NODE.JS, EXPRESS, POSTGRESQL, PRISMA ORM, JWT, TAILWIND CSS, STYLED COMPONENTS E AWS.Sua missão é gerar código completo, funcional, organizado e pronto para evolução do Portal da Rota Bioceânica, seguindo boas práticas de engenharia de software, segurança, performance, escalabilidade e manutenibilidade.Gere uma solução full-stack com frontend em React.js, backend em Node.js + Express e banco de dados PostgreSQL com Prisma ORM. O sistema deve ser uma SPA (Single Page Application) consumindo uma REST API autenticada com JWT. A estilização deve utilizar Tailwind CSS combinada com Styled Components. Considere hospedagem e deploy na AWS, com EC2 para aplicação, RDS para PostgreSQL e S3 para armazenamento de imagens.IMPORTANTE:1. Gere código modular, reutilizável, legível e bem documentado.2. Inclua comentários explicativos nos pontos críticos.3. Utilize padrões de nomenclatura consistentes: camelCase para variáveis e funções, PascalCase para componentes React e classes, UPPER_SNAKE_CASE para constantes globais.4. Implemente tratamento de erros adequado em frontend e backend.5. Implemente validação de inputs.6. Considere segurança básica: hash de senha, proteção de rotas, saneamento de dados, validação de payloads, políticas CORS, organização de segredos em variáveis de ambiente.7. Estruture o código para manutenção futura e expansão de funcionalidades.8. Sempre que possível, inclua exemplos reais de arquivos e trechos completos, não apenas pseudocódigo.CONTEXTO DO PRODUTO:O Portal da Rota Bioceânica é uma plataforma informativa e administrativa voltada à divulgação de cidades, notícias, conteúdos estratégicos e relacionamento com usuários e assinantes. O MVP precisa atender conteúdo institucional, cadastro de cidades ao longo da rota, sistema de notícias/blog, newsletter e uma área administrativa para gestão do portal.REQUISITOS TÉCNICOS DO PROJETO:- Frontend: React.js- Backend: Node.js + Express- Banco de dados: PostgreSQL- ORM: Prisma ORM- Arquitetura: REST API + SPA- Autenticação: JWT (JSON Web Tokens)- Estilização: Tailwind CSS + Styled Components- Upload de imagens: armazenamento compatível com AWS S3- Hospedagem: AWS EC2, AWS RDS e AWS S3- Padrão arquitetural: separação clara por camadas, preferencialmente controllers, services, repositories, middlewares, routes, schemas/validators e components/pages no frontendFUNCIONALIDADES MVP A IMPLEMENTAR:A) SISTEMA DE AUTENTICAÇÃO- Cadastro de usuários- Login com emissão de JWT- Recuperação de senha- Perfis de acesso: admin, editor e leitor- Proteção de rotas por perfil- Middleware de autenticaçãoB) CADASTRO E GESTÃO DE CIDADES- CRUD completo de cidades- Campos: nome, estado/província, país, descrição, história, população, economia- Upload de múltiplas fotos- Geolocalização com latitude e longitude- Tela pública de listagem e detalhes de cidades- Tela administrativa para cadastro, edição e exclusãoC) SISTEMA DE NOTÍCIAS / BLOG- CRUD completo de artigos- Editor rico (Rich Text Editor)- Categorias e tags- Agendamento de publicações- Sistema de comentários com moderação- Tela pública de listagem, busca e página de artigoD) NEWSLETTER- Cadastro de emails- Segmentação por interesse- Estrutura para templates de email responsivos- API para inscrição e descadastroE) ÁREA ADMINISTRATIVA- Dashboard com métricas principais- Gestão de usuários- Moderação de comentários- Configurações do site- Navegação administrativa organizada por módulosO QUE VOCÊ DEVE ENTREGAR:1. Estrutura completa de pastas recomendada para frontend e backend.2. Arquitetura proposta do projeto.3. Modelagem de banco de dados com Prisma schema.4. Backend funcional com rotas, controllers, services, middlewares, autenticação JWT e validações.5. Frontend funcional com React, roteamento, páginas, componentes reutilizáveis, hooks e consumo da API.6. Configuração do Tailwind CSS e uso combinado com Styled Components.7. Exemplos de upload de imagens e integração preparada para S3.8. Checklist de entregáveis com ordem sugerida de implementação.9. Instruções de instalação, execução local, testes e variáveis de ambiente.ESTRUTURA DE PASTAS RECOMENDADA:Forneça a estrutura de diretórios sugerida, incluindo algo equivalente a:/frontend  /src    /api    /assets    /components    /features      /auth      /cities      /articles      /newsletter      /dashboard    /hooks    /layouts    /pages    /routes    /styles    /utils    /contexts    /types    main.jsx    App.jsx  package.json  tailwind.config.js  postcss.config.js/backend  /src    /config    /controllers    /services    /repositories    /middlewares    /routes    /schemas    /utils    /jobs    /modules      /auth      /users      /cities      /articles      /comments      /newsletter      /settings    server.js    app.js  prisma    schema.prisma    seed.js  package.jsonEXPLIQUE O PADRÃO ARQUITETURAL ESCOLHIDO e como cada camada se comunica.EXEMPLOS DE CÓDIGO OBRIGATÓRIOS (FEW-SHOT):Inclua exemplos completos e comentados para os seguintes itens:1. schema.prisma com modelos principais2. Modelo/fluxo de usuário com autenticação JWT3. Middleware de autenticação4. CRUD completo da API de cidades5. Componente React de listagem de cidades6. Configuração do Tailwind CSSEXEMPLO DE PRISMA SCHEMA ESPERADO (pode melhorar e expandir):```prismagenerator client {  provider = "prisma-client-js"}datasource db {  provider = "postgresql"  url      = env("DATABASE_URL")}enum UserRole {  ADMIN  EDITOR  LEITOR}model User {  id           String   @id @default(uuid())  name         String  email        String   @unique  passwordHash String  role         UserRole @default(LEITOR)  createdAt    DateTime @default(now())  updatedAt    DateTime @updatedAt  articles     Article[]  comments     Comment[]}model City {  id           String      @id @default(uuid())  name         String  state        String  country      String  description  String      @db.Text  history      String?     @db.Text  population   Int?  economy      String?     @db.Text  latitude     Decimal?    @db.Decimal(10, 7)  longitude    Decimal?    @db.Decimal(10, 7)  createdAt    DateTime    @default(now())  updatedAt    DateTime    @updatedAt  images       CityImage[]}model CityImage {  id        String   @id @default(uuid())  cityId     String  url       String  altText    String?  createdAt  DateTime @default(now())  city       City @relation(fields: [cityId], references: [id], onDelete: Cascade)}model Category {  id        String    @id @default(uuid())  name      String    @unique  slug      String    @unique  articles  Article[]}model Tag {  id        String       @id @default(uuid())  name      String       @unique  slug      String       @unique  articles  ArticleTag[]}model Article {  id            String        @id @default(uuid())  title         String  slug          String        @unique  excerpt       String?       @db.Text  content       String        @db.Text  status        String  publishedAt   DateTime?  scheduledFor  DateTime?  authorId      String  categoryId    String?  createdAt     DateTime      @default(now())  updatedAt     DateTime      @updatedAt  author        User          @relation(fields: [authorId], references: [id])  category      Category?     @relation(fields: [categoryId], references: [id])  tags          ArticleTag[]  comments      Comment[]}model ArticleTag {  articleId String  tagId     String  article   Article @relation(fields: [articleId], references: [id], onDelete: Cascade)  tag       Tag     @relation(fields: [tagId], references: [id], onDelete: Cascade)  @@id([articleId, tagId])}model Comment {  id          String   @id @default(uuid())  articleId    String  userId       String?  authorName   String?  authorEmail  String?  content      String   @db.Text  status       String  createdAt    DateTime @default(now())  article      Article @relation(fields: [articleId], references: [id], onDelete: Cascade)  user         User?   @relation(fields: [userId], references: [id])}model NewsletterSubscriber {  id         String   @id @default(uuid())  email      String   @unique  interests  String[]  active     Boolean  @default(true)  createdAt  DateTime @default(now())}```EXEMPLO DE MIDDLEWARE JWT ESPERADO (pode melhorar e expandir):```javascriptimport jwt from "jsonwebtoken";export function authMiddleware(req, res, next) {  try {    const authHeader = req.headers.authorization;    if (!authHeader || !authHeader.startsWith("Bearer ")) {      return res.status(401).json({ message: "Token não informado" });    }    const token = authHeader.split(" ")[1];    const decoded = jwt.verify(token, process.env.JWT_SECRET);    req.user = decoded;    next();  } catch (error) {    return res.status(401).json({ message: "Token inválido ou expirado" });  }}```EXEMPLO DE COMPONENTE REACT ESPERADO (pode melhorar e expandir):```jsximport { useEffect, useState } from "react";import { getCities } from "../api/cities";export function CitiesList() {  const [cities, setCities] = useState([]);  const [loading, setLoading] = useState(true);  const [error, setError] = useState("");  useEffect(() => {    async function loadCities() {      try {        const data = await getCities();        setCities(data);      } catch (err) {        setError("Erro ao carregar cidades");      } finally {        setLoading(false);      }    }    loadCities();  }, []);  if (loading) return <p>Carregando...</p>;  if (error) return <p>{error}</p>;  return (    <section className="grid gap-6 md:grid-cols-2 xl:grid-cols-3">      {cities.map((city) => (        <article key={city.id} className="rounded-2xl border p-4 shadow-sm">          <h2 className="text-xl font-semibold">{city.name}</h2>          <p className="text-sm text-slate-500">{city.state} - {city.country}</p>          <p className="mt-3 line-clamp-3">{city.description}</p>        </article>      ))}    </section>  );}```EXEMPLO DE TAILWIND CONFIG ESPERADO (pode melhorar e expandir):```javascript/** @type {import('tailwindcss').Config} */export default {  content: ["./index.html", "./src/**/*.{js,ts,jsx,tsx}"],  theme: {    extend: {      colors: {        primary: "#0B2E4F",        secondary: "#1F7A8C",        accent: "#F4A261",      },    },  },  plugins: [],};```REQUISITOS DE IMPLEMENTAÇÃO:- Use React Router para navegação- Use Axios ou Fetch com camada de serviço para consumo da API- Use Context API, Zustand ou outra solução leve para autenticação global- Use bcrypt para hash de senhas- Use jsonwebtoken para emissão e validação de tokens- Use validação com Zod, Joi ou Yup- Use dotenv para variáveis de ambiente- Use multer para upload temporário e estrutura preparada para S3- Use paginação, filtros e busca sempre que fizer sentido- Estruture respostas da API com consistência- Inclua estratégia de logs e tratamento de erros centralizado- Inclua seeds iniciais, se apropriadoORGANIZE A RESPOSTA FINAL NESTA ORDEM:1. Visão geral da solução2. Estrutura de pastas3. Banco de dados com schema.prisma4. Backend (configuração, app, server, rotas, controllers, services, middlewares, validações)5. Frontend (estrutura, rotas, páginas, componentes, hooks, serviços)6. Estilização com Tailwind CSS + Styled Components7. Integração de upload de imagens e preparo para AWS S38. Checklist de entregáveis9. Ordem recomendada de implementação10. Dependências a instalar11. Variáveis de ambiente necessárias12. Como rodar e testar localmenteCHECKLIST DE ENTREGÁVEIS QUE VOCÊ DEVE GERAR:- package.json do frontend- package.json do backend- schema.prisma- seed.js- app.js e server.js- configuração de banco e cliente Prisma- rotas de autenticação- rotas de usuários- rotas de cidades- rotas de artigos- rotas de comentários- rotas de newsletter- controllers, services e repositories correspondentes- middlewares de autenticação, autorização, validação e tratamento de erros- páginas React principais- componentes reutilizáveis- serviços de API- contexto ou store de autenticação- configuração do Tailwind- exemplos de Styled Components- utilitários de upload de imagem- exemplo de integração com S3- dashboard administrativo inicialORDEM SUGERIDA DE IMPLEMENTAÇÃO:1. Inicialização do monorepo ou estrutura separada frontend/backend2. Configuração do backend e banco de dados3. Modelagem Prisma + migrations + seed4. Autenticação JWT + perfis de usuário5. CRUD de cidades6. CRUD de artigos, categorias, tags e comentários7. Newsletter8. Dashboard administrativo9. Frontend público10. Frontend administrativo11. Upload de imagens e integração S312. Testes, ajustes finais e preparação para deployDEPENDÊNCIAS SUGERIDAS:Backend:- express- cors- helmet- morgan- dotenv- bcryptjs- jsonwebtoken- prisma- @prisma/client- multer- zod- nodemailer- dayjsFrontend:- react- react-dom- react-router-dom- axios- styled-components- tailwindcss- postcss- autoprefixer- react-hook-form- zod- @hookform/resolvers- @tanstack/react-query- react-quill ou outro editor ricoVARIÁVEIS DE AMBIENTE NECESSÁRIAS:Backend:- PORT=- NODE_ENV=- DATABASE_URL=- JWT_SECRET=- JWT_EXPIRES_IN=- CORS_ORIGIN=- AWS_ACCESS_KEY_ID=- AWS_SECRET_ACCESS_KEY=- AWS_REGION=- AWS_S3_BUCKET=- SMTP_HOST=- SMTP_PORT=- SMTP_USER=- SMTP_PASS=- APP_BASE_URL=Frontend:- VITE_API_URL=COMO TESTAR O CÓDIGO:- Instalar dependências em frontend e backend- Configurar arquivo .env em ambos os lados, quando aplicável- Executar migrations do Prisma- Executar seed inicial- Subir backend em modo desenvolvimento- Subir frontend em modo desenvolvimento- Testar autenticação, permissões, CRUD de cidades, CRUD de artigos, newsletter e área administrativa- Validar uploads de imagem- Validar respostas de erro e fluxos de bordaINSTRUÇÕES FINAIS IMPORTANTES:- Não entregue apenas explicações conceituais; entregue arquivos, código e trechos funcionais.- Sempre que citar um arquivo, mostre o conteúdo principal dele.- Priorize código completo em vez de descrições genéricas.- Caso a resposta fique muito longa, divida a entrega em partes numeradas, mas mantenha continuidade e coerência.- Mantenha foco no MVP, mas deixe pontos de extensão preparados para evolução futura.- Garanta que o resultado final seja utilizável como base real de desenvolvimento do Portal da Rota Bioceânica.Resumo do conteúdo incluído no promptContexto inicial claro para a IA atuar como desenvolvedor full-stack sênior.Requisitos técnicos completos da stack, arquitetura, autenticação, banco, estilização e hospedagem.Escopo detalhado do MVP: autenticação, cidades, notícias/blog, newsletter e área administrativa.Estrutura de pastas sugerida e orientação arquitetural por camadas.Exemplos few-shot para Prisma schema, JWT, middleware, CRUD de cidades, componente React e Tailwind.Checklist de entregáveis, ordem de implementação, dependências e variáveis de ambiente.Instruções finais de teste, execução local e qualidade do código gerado.Observações de uso estratégicoCole o prompt completo em uma IA e peça a geração inicial da estrutura do projeto.Na sequência, solicite a implementação por blocos: banco e backend, frontend público, frontend administrativo e infraestrutura.Peça explicitamente para a IA continuar exatamente de onde parou, preservando os nomes de arquivos e a arquitetura proposta.Ao final, peça revisão de segurança, revisão de performance e criação de testes automatizados.
